import { Resource } from "../../lib";
export default class User extends Resource {
    _id: string;
    username: string;
    email: string;
    password: string;
    static find({ _id }: {
        _id: any;
    }): Promise<any>;
    static getAll(): Promise<import("../../lib").Collection>;
    static register({ username, email, password }: {
        username: any;
        email: any;
        password: any;
    }): Promise<User>;
    static update({ _id, email }: {
        _id: any;
        email: any;
    }): Promise<any>;
    static remove({ _id }: {
        _id: any;
    }): Promise<any>;
    static authenticate({ username, password }: {
        username: any;
        password: any;
    }): Promise<any>;
}
