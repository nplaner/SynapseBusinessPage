import { Resource, Collection } from "../../lib";
export default class Comment extends Resource {
    id: string;
    text: string;
    static Last(): any;
    static Find({ id }: {
        id: any;
    }): any;
    static List({ index }: {
        index: any;
    }): Collection;
    static Post({ text }: {
        text: any;
    }): Promise<Comment>;
}
