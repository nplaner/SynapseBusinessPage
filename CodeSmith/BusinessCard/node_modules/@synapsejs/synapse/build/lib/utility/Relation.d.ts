export default class Relation<F, T> {
    fromMap: Map<F, Set<T>>;
    toMap: Map<T, Set<F>>;
    link(from: F, to: T): void;
    unlink(from?: F, to?: T): void;
    from(val: F): Array<T>;
    to(val: T): Array<F>;
}
